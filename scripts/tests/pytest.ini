[pytest]
markers = 
    dependency: mark a test that other tests depend on
    production: mark a test as a production test (comprehensive and potentially slow)
    
# Exclude production tests by default
addopts = -m "not production"

# Test file patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_* 